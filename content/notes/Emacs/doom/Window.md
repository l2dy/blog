`SPC w o` (doom/window-enlargen)
`SPC w C-o` (delete-other-windows)
`SPC w d` (+workspace/close-window-or-workspace), also `s-w` and `SPC w c`

`SPC w q` (evil-quit = `Z Q`)

Note: `SPC w` and `C-w` are interchangeable.